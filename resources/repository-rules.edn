;; This repository's entry in the kotoba.repository-role-rules taxonomy.
;; Declared here (not merely inherited) so a reader of THIS repo alone can
;; verify which contract it claims to satisfy. ADR-2607266000.
{:schema "kotoba.repository-role-rules.v0"
 :this-repo
 {:name "provider"
  :tier :t3
  :role :provider
  :must-not [:be-linked-into-a-compiler :grant-itself-authority :expose-ambient-WASI]
  ;; Compared by verify-repository-roles.cljs against every :git/url in
  ;; production :deps, so a dependency added there has to be added here in the
  ;; same commit. kgraph stood in deps.edn and not here.
  :depends-on ["kotoba-lang/kgraph"
               "kotoba-lang/kotoba-kir"]}}
