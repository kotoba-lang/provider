{:kotoba.capability-kit/name :random-bytes
 :kotoba.capability-kit/version 1
 :capability {:name :random/bytes :id :pure-allowlist}
 :semantics {:operation :random-bytes
             :ambient-crypto false
             :destination-authority :in-module
             :execution :synchronous-reference
             :capability-package "kotoba-lang/capability-random-bytes"}
 :limits {:note "see capability package"}
 :request [:record :kotoba.random-bytes/request []]
 :result [:i64]
 :transports
 {:wasm-core :reference-implemented
  :jvm-host :reference-implemented}
 :qualification {:reference :implemented
                 :wasm-aot :partial
                 :native-aot :pending
                 :jit :pending
                 :signed-content-addressed-package :pending
                 :notes "Random bytes wasm core (capability-random-bytes#1). Signed production claim still pending readiness :signed-wasm + grant-binding (ADR 0160)."}}
