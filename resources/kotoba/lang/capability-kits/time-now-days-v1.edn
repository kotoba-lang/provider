{:kotoba.capability-kit/name :time-now-days
 :kotoba.capability-kit/version 1
 :capability {:name :time/now-days :id :pure-allowlist}
 :semantics {:operation :now-days
             :ambient-crypto false
             :destination-authority :in-module
             :execution :synchronous-reference
             :capability-package "kotoba-lang/capability-time-now-days"}
 :limits {:note "see capability package"}
 :request [:record :kotoba.time-now-days/request []]
 :result [:i64]
 :transports
 {:wasm-core :reference-implemented
  :jvm-host :reference-implemented}
 :qualification {:reference :implemented
                 :wasm-aot :partial
                 :native-aot :pending
                 :jit :pending
                 :signed-content-addressed-package :pending
                 :notes "UTC day counter wasm core (capability-time-now-days#1). Signed production claim still pending readiness :signed-wasm + grant-binding (ADR 0160)."}}
