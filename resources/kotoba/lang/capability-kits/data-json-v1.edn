{:kotoba.capability-kit/name :data-json
 :kotoba.capability-kit/version 1
 :capability {:name :data/json :id :pure-allowlist}
 :semantics {:operation :json-encode
             :ambient-crypto false
             :destination-authority :in-module
             :execution :synchronous-reference
             :capability-package "kotoba-lang/capability-data-json"}
 :limits {:note "see capability package"}
 :request [:record :kotoba.data-json/request []]
 :result [:i64]
 :transports
 {:wasm-core :reference-implemented
  :jvm-host :reference-implemented}
 :qualification {:reference :implemented
                 :wasm-aot :partial
                 :native-aot :pending
                 :jit :pending
                 :signed-content-addressed-package :pending
                 :notes "Pure JSON encode/extract wasm core (capability-data-json#1). Signed production claim still pending readiness :signed-wasm + grant-binding (ADR 0160)."}}
