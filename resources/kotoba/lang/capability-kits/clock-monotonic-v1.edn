{:kotoba.capability-kit/name :clock-monotonic
 :kotoba.capability-kit/version 1
 :capability {:name :clock/monotonic :id :pure-allowlist}
 :semantics {:operation :clock-monotonic
             :ambient-crypto false
             :destination-authority :in-module
             :execution :synchronous-reference
             :capability-package "kotoba-lang/capability-clock-monotonic"}
 :limits {:note "see capability package"}
 :request [:record :kotoba.clock-monotonic/request []]
 :result [:i64]
 :transports
 {:wasm-core :reference-implemented
  :jvm-host :reference-implemented}
 :qualification {:reference :implemented
                 :wasm-aot :partial
                 :native-aot :pending
                 :jit :pending
                 :signed-content-addressed-package :pending
                 :notes "Monotonic clock wasm core (capability-clock-monotonic#1). Signed production claim still pending readiness :signed-wasm + grant-binding (ADR 0160)."}}
