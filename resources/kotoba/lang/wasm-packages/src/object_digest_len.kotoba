(ns kotoba.object.digest-len
  ;; ADR 0275: pure object digest length bound (non-empty, max string limit).
  (:export [object_digest_len_ok]))

(defn object_digest_len_ok
  [digest-len :i64]
  :i64
  (if (<= digest-len 0)
    -1
    (if (> digest-len 65536)
      -2
      0)))
