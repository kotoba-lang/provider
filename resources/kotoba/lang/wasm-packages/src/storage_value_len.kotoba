(ns kotoba.storage.value-len
  ;; ADR 0275: pure storage value-bytes bound (max-value-bytes 65536).
  (:export [storage_value_len_ok]))

(defn storage_value_len_ok
  [value-len :i64]
  :i64
  (if (< value-len 0)
    -1
    (if (> value-len 65536)
      -2
      0)))
