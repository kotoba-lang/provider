(ns kotoba.secret.value-len
  (:export [secret_value_len_ok]))

;; Pure secret-v1 :limits :secret-bytes 8192 (ADR 0174 companion to 0173 name-len).
(defn secret_value_len_ok
  [secret-len :i64]
  :i64
  (if (< secret-len 0)
    -1
    (if (> secret-len 8192)
      -2
      0)))
